use crate::imagery::Imagery;

pub struct GlobeSurfaceTileProvider {
    lightingFadeOutDistance: f32,
    lightingFadeInDistance: f32,
}
pub fn compute_tile_visibility(tile: Imagery) {}
pub fn compute_distance_to_tile(tile: Imagery) {}
